stackbitVersion: ~0.3.0
ssgName: custom
buildCommand: npx @11ty/eleventy
publishDir: _site
pagesDir: content
staticDir: static
uploadDir: images
pageLayoutKey: layout
# Models
models:
  # Pages
  home:
    type: page
    label: Home
    layout: layout.njk
    fields:
    - type: string
      name: title
      label: Title